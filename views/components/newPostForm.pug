form(action="/newMessage" method="post" class="styledForm") 
    h2 New message form
    .formComponent 
        label(for="title") Title:
        input(type="text" name="postTitle")
    .formComponent 
        label(for="message") Message:
        textarea(name="postMessage") 
    button(type="submit") Submit